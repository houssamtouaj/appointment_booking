# ---------------------------------------------------------------------
# Local planning docs & UML — excluded until the diagrams are reviewed.
# Delete these two lines to publish them (they are good portfolio material;
# docs/project-brief.md is the one that should stay private).
# ---------------------------------------------------------------------
docs/

# ---------------------------------------------------------------------
# Backend — Java / Maven
# ---------------------------------------------------------------------
target/
build/
.mvn/timing.properties
*.class
*.jar
*.war
*.ear
hs_err_pid*
replay_pid*

# ---------------------------------------------------------------------
# Frontend — Node / Vite
# ---------------------------------------------------------------------
node_modules/
dist/
dist-ssr/
.vite/
coverage/
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.eslintcache

# Playwright's own output (wave 4). `test-results/` holds the trace, video and
# screenshot kept from a failed run and is worth minutes of disk; `playwright-report/`
# is the HTML viewer for it. Both are regenerated by `npm run e2e` and neither is
# reviewable in a diff. The browsers themselves live outside the repository.
frontend/playwright-report/
frontend/test-results/
frontend/blob-report/
.playwright/

# ---------------------------------------------------------------------
# Secrets & environment
# Never commit Stripe keys, JWT secrets, or DB credentials.
# ---------------------------------------------------------------------
.env
.env.*
!.env.example
# application-local.yml IS committed: it only points at the Compose services and
# holds no credentials. Personal overrides belong in application-local-<name>.yml.
application-local-*.yml
application-local-*.properties
*.pem
*.key

# ---------------------------------------------------------------------
# IDE & editor
# ---------------------------------------------------------------------
.idea/
*.iml
*.iws
*.ipr
.vscode/
!.vscode/extensions.json
.settings/
.project
.classpath
*.swp
*.swo

# ---------------------------------------------------------------------
# OS
# ---------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
